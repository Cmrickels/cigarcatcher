{% extends 'base.html.twig' %}

{% block body %}
    <h1>Shape</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shape.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shape.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shape.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ shape.image }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('shape_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('shape_edit', { 'id': shape.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
