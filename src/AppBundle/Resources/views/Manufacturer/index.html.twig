{% extends 'admin-base.html.twig' %}

{% block title %}
    Manufacturer List
{% endblock %}

{% block stylesheets %}
    <style>



    </style>
{% endblock %}

{% block javascripts %}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-2"><h2>Manufacturers</h2></div>
        </div>
        <div class="row">
            <input type="button" class="btn pull-right btn-default" onclick="location.href = '{{ path('manufacturer_new') }}'" value="Create new size">
        </div>size_new
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for manufacturer in manufacturers %}
                    <tr>
                        <td>{{ manufacturer.id }}</td>
                        <td>{{ manufacturer.name }}</td>
                        <td>{{ manufacturer.description }}</td>
                        <td><img style="max-height:150px;" src="{{ asset('uploads/manufacturer/' ~ manufacturer.image) }}"></td>
                        <td><a href="{{ path('manufacturer_index_delete', {'id' : manufacturer.id}) }}" class="btn btn-danger">Delete</a></td>
                        <td><a href="{{ path('manufacturer_edit', {'id' : manufacturer.id} ) }}" class="btn btn-default">Edit</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}